spring:
  application:
    name: test-project-service2
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/service2
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: none

server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8751/eureka